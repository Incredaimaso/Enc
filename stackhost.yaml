user:
  name: cevilion 
  github: incredaimaso

language: "python"

commands:
  build:
    - "pip3 install -r requirements.txt"
    - "pip3 install ffmpeg-python"
  package:
    - "nano inxi"
  start:
    - "python3 -m bot"
  env:
  - "ENVIRONMENT=production"
